name: H+ Astro Preview CI

on:
  push:
    branches:
      - preview/hplus-astro-resume
  pull_request:
    paths:
      - 'astro-preview/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: astro-preview
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: astro-preview/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Build Astro preview
        run: npm run build
