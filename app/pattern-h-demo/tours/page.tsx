import type{Metadata}from'next';import DemoNav from'../demo-nav';import styles from'../pattern-h-demo.module.css';import scrap from'../scrapbook.module.css';import{routes}from'../site-data';export const metadata:Metadata={title:'Tours — Uma Academia Pattern H Demo',robots:{index:false,follow:false}};export default function Tours(){return <main className={styles.page}><DemoNav/><section className={styles.statement}><p className={styles.sectionNo}>TOURS</p><h2>Choose the day<br/>you can picture.</h2><div><p className={styles.bigText}>Concrete Kyoto routes — time, price, pace and places included. Every route is a starting point, not a rigid package.</p></div></section><section className={scrap.routeGallery}>{routes.map((r,i)=><article className={scrap.routeCard} key={r.slug}><figure><img src={r.image} alt="Kyoto tour scene"/></figure><div><p className={styles.kicker}>0{i+1} / PRIVATE KYOTO</p><h3>{r.title}</h3><p><strong>{r.time} · {r.price}</strong></p><p><strong>Best for:</strong> {r.bestFor}</p><p><strong>Example route:</strong> {r.flow}</p><p>{r.text}</p><a href="/pattern-h-demo/enquire">Ask about this route →</a></div></article>)}</section></main>}